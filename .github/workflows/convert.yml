name: convert secret to env varilable

on:
  push:
  pull_request:
    
jobs: 
    generate-secret-output:
        runs-on: ubuntu-latest
        steps:
        - name: Generate env from secret
          uses: ./.github/workflows/convert@main
          env:
            API_KEY: ${{ secrets.SECRET_API_KEY}};
